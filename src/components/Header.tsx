import styled from "styled-components";

const Title = styled.h1`
  font-weight: lighter;
  color: bisque;
  font-size: 12rem;
  margin: 0;
`

const Header = () => <header><Title>todos</Title></header>

export default Header